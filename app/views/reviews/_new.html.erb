<%= simple_form_for [cocktail, review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (1..5).to_a %>
  <%= f.submit "Leave a review", class: "btn btn-dark btn btn-block" %>
<% end %>
